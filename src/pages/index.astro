---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { getCollection } from 'astro:content';
import FormattedDate from '../components/FormattedDate.astro';

// Get latest 3 posts
const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
).slice(0, 3);
---

<!doctype html>
<html lang="zh-Hant">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
            /* Ali Abdaal Inspired Styles */
            :root {
                --ali-accent: #2563EB; /* Friendly Blue or keep Red? User brand is Red. I'll stick to Vista Red for CTA, but layout changes */
                --ali-bg: #FFFFFF;
                --ali-text: #171717;
            }

            .container {
                max-width: 1200px;
                margin: 0 auto;
                padding: 0 1.5rem;
            }

            /* HERO SECTION */
            .hero {
                padding: 6rem 0;
                display: grid;
                grid-template-columns: 1.2fr 1fr; /* Text larger, Image smaller */
                gap: 4rem;
                align-items: center;
            }
            @media (max-width: 900px) {
                .hero {
                    grid-template-columns: 1fr;
                    text-align: center;
                    padding: 4rem 0;
                }
                .hero-image { order: -1; margin-bottom: 2rem; }
            }

            .hero-content h1 {
                font-family: var(--font-heading);
                font-size: 3.5rem;
                font-weight: 800;
                line-height: 1.1;
                margin-bottom: 1.5rem;
                color: var(--black);
            }
            .hero-content p {
                font-family: var(--font-body);
                font-size: 1.25rem;
                line-height: 1.6;
                color: var(--gray-dark);
                margin-bottom: 2.5rem;
                max-width: 600px;
            }

            .hero-image img {
                width: 100%;
                border-radius: 12px;
                transform: rotate(2deg); /* Playful tilt */
                transition: transform 0.3s ease;
                box-shadow: 20px 20px 0 var(--color-warm-bg); /* Graphical element */
            }
            .hero-image:hover img {
                transform: rotate(0deg);
            }
            
            /* Newsletter Form in Hero */
            .hero-newsletter {
                background: #f9f9f9;
                padding: 1.5rem;
                border-radius: 8px;
                border: 1px solid #eee;
                max-width: 500px;
            }
            @media (max-width: 900px) { .hero-newsletter { margin: 0 auto; } }
            
            .newsletter-form {
                display: flex;
                gap: 0.5rem;
            }
            .newsletter-input {
                flex: 1;
                padding: 0.8rem 1rem;
                border: 1px solid #ddd;
                border-radius: 6px;
                font-size: 1rem;
            }
            .newsletter-btn {
                background: var(--color-red);
                color: white;
                border: none;
                padding: 0.8rem 1.5rem;
                border-radius: 6px;
                font-weight: 600;
                cursor: pointer;
                transition: background 0.2s;
            }
            .newsletter-btn:hover { background: #b91c1c; }
            .newsletter-caption {
                font-size: 0.85rem;
                color: #666;
                margin-top: 0.8rem;
            }

            /* SECTIONS */
            .section-header {
                text-align: center;
                margin-bottom: 3rem;
            }
            .section-header h2 {
                font-size: 2.2rem;
                font-weight: 700;
                margin-bottom: 0.5rem;
            }
            .section-header p {
                color: #666;
                font-size: 1.1rem;
            }

            /* LATEST ARTICLES */
            .latest-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
                gap: 2rem;
                margin-bottom: 6rem;
            }
            .article-card {
                background: white;
                border: 1px solid #f0f0f0;
                border-radius: 8px;
                overflow: hidden;
                transition: transform 0.2s, box-shadow 0.2s;
            }
            .article-card:hover {
                transform: translateY(-4px);
                box-shadow: 0 10px 30px rgba(0,0,0,0.08);
            }
            .article-image {
                height: 200px;
                overflow: hidden;
            }
            .article-image img {
                width: 100%;
                height: 100%;
                object-fit: cover;
            }
            .article-content {
                padding: 1.5rem;
            }
            .article-tag {
                font-size: 0.75rem;
                text-transform: uppercase;
                letter-spacing: 0.05em;
                color: var(--color-red);
                font-weight: bold;
                margin-bottom: 0.5rem;
                display: block;
            }
            .article-title {
                font-size: 1.25rem;
                font-weight: 700;
                margin: 0 0 0.5rem 0;
                line-height: 1.4;
            }
            .article-title a {
                color: var(--black);
                text-decoration: none;
            }
            .article-date {
                font-size: 0.85rem;
                color: #888;
            }

            /* FEATURED SECTION (Book/Products) */
            .featured-section {
                background: var(--color-warm-bg);
                padding: 6rem 0;
                margin: 4rem 0;
            }
            .featured-grid {
                display: grid;
                grid-template-columns: 1fr 1fr;
                gap: 4rem;
                align-items: center;
            }
             @media (max-width: 900px) { .featured-grid { grid-template-columns: 1fr; } }
		</style>
	</head>
	<body>
		<Header />
		<main>
            
            <!-- HERO -->
            <div class="container hero">
                <div class="hero-content">
                    <h1>Hey, I'm Vista 👋</h1>
                    <p>
                        我是<b>講師、作家與顧問</b>。我透過文章、課程與電子報，協助你透過 <span style="color:var(--color-red); font-weight:bold;">AI 工具</span> 與 <span style="color:var(--color-red); font-weight:bold;">內容策略</span>，建立高效的個人知識系統與商業影響力。
                    </p>
                    
                    <!-- Newsletter Sign Up -->
                    <div class="hero-newsletter">
                        <form class="newsletter-form" action="https://iamvista.substack.com/" target="_blank">
                            <input type="email" placeholder="輸入你的 Email..." class="newsletter-input" required />
                            <button type="submit" class="newsletter-btn">免費訂閱</button>
                        </form>
                        <p class="newsletter-caption">加入 5,000+ 專業工作者的行列，每週獲得最新的 AI 與寫作洞察。</p>
                    </div>
                </div>
                
                <div class="hero-image">
                    <img src="/profile.jpg" alt="Vista Cheng" width="600" height="600" />
                </div>
            </div>

            <!-- LATEST ARTICLES -->
            <div class="container">
                <div class="section-header">
                    <h2>最新文章</h2>
                    <p>探索 AI、寫作與自我成長的最新觀點</p>
                </div>
                
                <div class="latest-grid">
                    {
                        posts.map((post) => (
                            <div class="article-card">
                                <a href={`/blog/${post.id}/`} style="text-decoration:none;">
                                    <div class="article-image">
                                        <img src={post.data.heroImage} alt="" />
                                    </div>
                                    <div class="article-content">
                                        <span class="article-tag">Article</span>
                                        <h3 class="article-title">{post.data.title}</h3>
                                        <FormattedDate date={post.data.pubDate} class="article-date" />
                                    </div>
                                </a>
                            </div>
                        ))
                    }
                </div>
                
                <div style="text-align: center; margin-bottom: 6rem;">
                     <a href="/blog" style="font-weight: 600; text-decoration: none; color: var(--color-black); border-bottom: 2px solid var(--color-red); padding-bottom: 2px;">閱讀所有文章 &rarr;</a>
                </div>
            </div>

            <!-- FEATURED / SERVICES -->
            <div class="featured-section">
                <div class="container featured-grid">
                    <div>
                         <h2 style="font-size: 2.5rem; margin-bottom: 1.5rem; font-family: var(--font-heading);">Start Here</h2>
                         <p style="font-size: 1.2rem; line-height: 1.6; color: #444; margin-bottom: 2rem;">
                             如果你是第一次來到這裡，我為你整理了一份導覽指南。這是我精選的熱門文章列表，涵蓋寫作技巧、AI 應用與更具生產力的生活方式。
                         </p>
                         <a href="/start-here" style="
                            display: inline-block;
                            padding: 1rem 2rem;
                            background: var(--black);
                            color: white;
                            text-decoration: none;
                            border-radius: 6px;
                            font-weight: bold;
                         ">開始探索</a>
                    </div>
                    <div>
                        <!-- Could be an image or a list of categories -->
                        <div style="background: white; padding: 2rem; border-radius: 12px; box-shadow: 0 10px 30px rgba(0,0,0,0.05);">
                            <h3 style="margin-top:0; margin-bottom:1rem;">熱門主題</h3>
                            <ul style="list-style: none; padding: 0; display: grid; gap: 1rem;">
                                <li><a href="/blog" style="color:var(--black); text-decoration:none; display:flex; justify-content:space-between; border-bottom:1px solid #eee; padding-bottom:0.8rem;"><span>✍️ 寫作技巧</span> <span>&rarr;</span></a></li>
                                <li><a href="/blog" style="color:var(--black); text-decoration:none; display:flex; justify-content:space-between; border-bottom:1px solid #eee; padding-bottom:0.8rem;"><span>🤖 AI 工作流</span> <span>&rarr;</span></a></li>
                                <li><a href="/blog" style="color:var(--black); text-decoration:none; display:flex; justify-content:space-between; padding-bottom:0.8rem;"><span>🚀 個人品牌</span> <span>&rarr;</span></a></li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>

		</main>
		<Footer />
	</body>
</html>
