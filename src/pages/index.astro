---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { getCollection } from 'astro:content';
import FormattedDate from '../components/FormattedDate.astro';

// Get all posts
const allPosts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

// Latest 3 posts
const posts = allPosts.slice(0, 3);

// Content Curated Lists
const writingPosts = allPosts.filter(p => p.data.tags && (p.data.tags.includes('寫作') || p.data.tags.includes('Writing') || p.data.tags.includes('寫作技巧'))).slice(0, 5);
const aiPosts = allPosts.filter(p => p.data.tags && (p.data.tags.includes('AI') || p.data.tags.includes('AI 工具') || p.data.tags.includes('Artificial Intelligence'))).slice(0, 5);
const brandPosts = allPosts.filter(p => p.data.tags && (p.data.tags.includes('個人品牌') || p.data.tags.includes('Personal Brand') || p.data.tags.includes('品牌行銷'))).slice(0, 5);

// Helper to get main tag
const getMainTag = (post) => {
    if (post.data.tags && post.data.tags.length > 0) return post.data.tags[0];
    return 'Article';
};
---

<!doctype html>
<html lang="zh-Hant">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
            /* Ali Abdaal Inspired Styles */
            :root {
                --ali-accent: #2563EB;
                --ali-bg: #FFFFFF;
                --ali-text: #171717;
            }

            .container {
                max-width: 1200px;
                margin: 0 auto;
                padding: 0 1.5rem;
            }

            /* HERO SECTION */
            .hero {
                padding: 6rem 0;
                display: grid;
                grid-template-columns: 1.2fr 1fr;
                gap: 4rem;
                align-items: center;
            }
            @media (max-width: 900px) {
                .hero {
                    grid-template-columns: 1fr;
                    text-align: center;
                    padding: 4rem 0;
                }
                .hero-image { order: -1; margin-bottom: 2rem; }
            }

            .hero-content h1 {
                font-family: var(--font-heading);
                font-size: 3.5rem;
                font-weight: 800;
                line-height: 1.1;
                margin-bottom: 1.5rem;
                color: var(--black);
            }
            .hero-content p {
                font-family: var(--font-body);
                font-size: 1.25rem;
                line-height: 1.6;
                color: var(--gray-dark);
                margin-bottom: 2.5rem;
                max-width: 600px;
            }

            .hero-image img {
                width: 100%;
                border-radius: 12px;
                transform: rotate(2deg); 
                transition: transform 0.3s ease;
                box-shadow: 20px 20px 0 var(--color-warm-bg); 
            }
            .hero-image:hover img {
                transform: rotate(0deg);
            }
            
            /* Newsletter Embed in Hero */
            .hero-newsletter {
                /* background: #f9f9f9; Removed background to blend iframe */
                padding: 0;
                /* border: 1px solid #eee; */
                max-width: 500px;
                overflow: hidden; /* contain iframe */
            }
            @media (max-width: 900px) { .hero-newsletter { margin: 0 auto; } }
            
            .newsletter-caption {
                font-size: 0.85rem;
                color: #666;
                margin-top: 1rem;
                text-align: left;
            }
            @media (max-width: 900px) { .newsletter-caption { text-align: center; } }


            /* SECTIONS */
            .section-header {
                text-align: center;
                margin-bottom: 3rem;
            }
            .section-header h2 {
                font-size: 2.2rem;
                font-weight: 700;
                margin-bottom: 0.5rem;
            }
            .section-header p {
                color: #666;
                font-size: 1.1rem;
                margin-bottom: 0;
            }
            
            /* SERVICES (Restored) */
            .grid-section {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
                gap: 2rem;
                margin-bottom: 4rem; 
            }
            .card {
                padding: 2rem;
                background: white;
                border-radius: 8px; /* Slightly more rounded */
                box-shadow: 0 4px 6px rgba(0,0,0,0.05); /* Softer shadow */
                transition: transform 0.2s, box-shadow 0.2s;
                border: 1px solid #eee;
            }
            .card:hover {
                transform: translateY(-4px);
                box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
                border-color: var(--color-red);
            }
            .card h3 {
                margin-top: 0;
                font-size: 1.4rem;
                color: var(--color-primary);
                margin-bottom: 1rem;
                font-weight: 700;
            }
            .card p {
                color: var(--gray-dark);
                line-height: 1.6;
                margin: 0;
            }

            /* LATEST ARTICLES */
            .latest-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
                gap: 2rem;
                margin-bottom: 6rem;
            }
            .article-card {
                background: white;
                border: 1px solid #f0f0f0;
                border-radius: 8px;
                overflow: hidden;
                transition: transform 0.2s, box-shadow 0.2s;
                display: flex;
                flex-direction: column;
                height: 100%;
            }
            .article-card:hover {
                transform: translateY(-4px);
                box-shadow: 0 10px 30px rgba(0,0,0,0.08);
            }
            .article-image {
                height: 200px;
                overflow: hidden;
            }
            .article-image img {
                width: 100%;
                height: 100%;
                object-fit: cover;
            }
            .article-content {
                padding: 1.5rem;
                flex: 1;
                display: flex;
                flex-direction: column;
            }
            .article-tag {
                font-size: 0.75rem;
                text-transform: uppercase;
                letter-spacing: 0.05em;
                color: var(--color-red);
                font-weight: bold;
                margin-bottom: 0.5rem;
                display: block;
            }
            .article-title {
                font-size: 1.25rem;
                font-weight: 700;
                margin: 0 0 0.5rem 0;
                line-height: 1.4;
                color: var(--black);
            }
            .article-date {
                font-size: 0.85rem;
                color: #888;
                margin-top: auto; /* Push date to bottom */
            }

            /* FEATURED SECTION (Book/Products) - Curated Lists */
            .featured-section {
                background: var(--color-warm-bg);
                padding: 6rem 0;
                margin: 4rem 0;
            }
            .featured-grid {
                display: grid;
                grid-template-columns: 1fr 2fr;
                gap: 4rem;
                align-items: start;
            }
             @media (max-width: 900px) { .featured-grid { grid-template-columns: 1fr; } }
             
            .curated-lists {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
                gap: 2rem;
            }
            
            .list-column h3 {
                font-size: 1.25rem;
                border-bottom: 2px solid #ddd;
                padding-bottom: 0.5rem;
                margin-bottom: 1.5rem;
                color: var(--black);
            }
            .list-column ul {
                list-style: none;
                padding: 0;
                display: flex;
                flex-direction: column;
                gap: 1rem;
            }
            .list-column a {
                text-decoration: none;
                color: var(--gray-dark);
                font-weight: 500;
                line-height: 1.4;
                display: block;
                border-bottom: 1px solid transparent;
                transition: all 0.2s;
            }
            .list-column a:hover {
                color: var(--color-red);
                transform: translateX(4px);
            }
		</style>
	</head>
	<body>
		<Header />
		<main>
            
            <!-- HERO -->
            <div class="container hero">
                <div class="hero-content">
                    <h1>Hey, I'm Vista 👋</h1>
                    <p>
                        我是<b>講師、作家與顧問</b>。我透過文章、課程與電子報，協助你透過 <span style="color:var(--color-red); font-weight:bold;">AI 工具</span> 與 <span style="color:var(--color-red); font-weight:bold;">內容策略</span>，建立高效的個人知識系統與商業影響力。
                    </p>
                    
                    <!-- Newsletter Embed (Substack Iframe) -->
                    <div class="hero-newsletter">
                        <iframe src="https://iamvista.substack.com/embed" width="100%" height="320" style="border:1px solid #EEE; background:white;" frameborder="0" scrolling="no"></iframe>
                        <p class="newsletter-caption">加入 15,541+ 專業工作者的行列，每週獲得最新的 AI 與寫作洞察。</p>
                    </div>
                </div>
                
                <div class="hero-image">
                    <img src="/profile.jpg" alt="Vista Cheng" width="600" height="600" />
                </div>
            </div>
            
            <!-- SERVICES (Added Back) -->
            <div class="container" style="margin-bottom: 2rem;">
                 <h2 class="section-title">核心服務</h2>
                 <div class="grid-section">
                    <div class="card">
                        <h3>✍️ 寫作陪伴</h3>
                        <p>拆解寫作焦慮，建立穩定產出的原子習慣。從部落格到出書，陪你走過創作長路。</p>
                    </div>
                    <div class="card">
                        <h3>🤖 AI 工作流</h3>
                        <p>不只是下指令，而是打造專屬於你的 AI 協作系統。提升 10 倍效率，專注在更有價值的事。</p>
                    </div>
                    <div class="card">
                        <h3>💼 企業內訓</h3>
                        <p>為企業團隊導入內容行銷與數位工具思維，提升組織整體的溝通與生產力。</p>
                    </div>
                </div>
            </div>

            <!-- LATEST ARTICLES -->
            <div class="container">
                <div class="section-header">
                    <h2>最新文章</h2>
                    <p>探索 AI、寫作與自我成長的最新觀點</p>
                </div>
                
                <div class="latest-grid">
                    {
                        posts.map((post) => (
                            <div class="article-card">
                                <a href={`/blog/${post.id}/`} style="text-decoration:none; display:flex; flex-direction:column; height:100%;">
                                    <div class="article-image">
                                        <img src={post.data.heroImage} alt="" />
                                    </div>
                                    <div class="article-content">
                                        {/* Dynamic Tag */}
                                        <span class="article-tag">{getMainTag(post)}</span>
                                        <h3 class="article-title">{post.data.title}</h3>
                                        <FormattedDate date={post.data.pubDate} class="article-date" />
                                    </div>
                                </a>
                            </div>
                        ))
                    }
                </div>
                
                <div style="text-align: center; margin-bottom: 6rem;">
                     <a href="/blog" style="font-weight: 600; text-decoration: none; color: var(--color-black); border-bottom: 2px solid var(--color-red); padding-bottom: 2px;">閱讀所有文章 &rarr;</a>
                </div>
            </div>

            <!-- START HERE & CURATED LISTS -->
            <div class="featured-section">
                <div class="container featured-grid">
                    <!-- Left: Start Here Intro -->
                    <div>
                         <h2 style="font-size: 2.5rem; margin-bottom: 1.5rem; font-family: var(--font-heading);">從這裡開始</h2>
                         <p style="font-size: 1.2rem; line-height: 1.6; color: #444; margin-bottom: 2rem;">
                             如果你是第一次來到這裡，我為你整理了一份導覽指南。這是我精選的熱門文章列表，涵蓋寫作技巧、AI 應用與個人品牌的精華內容。
                         </p>
                         <a href="/start-here" style="
                            display: inline-block;
                            padding: 1rem 2rem;
                            background: var(--black);
                            color: white;
                            text-decoration: none;
                            border-radius: 6px;
                            font-weight: bold;
                         ">開始探索 &rarr;</a>
                    </div>
                    
                    <!-- Right: Curated Lists -->
                    <div class="curated-lists">
                        <!-- Writing -->
                        <div class="list-column">
                            <h3>✍️ 寫作技巧</h3>
                            <ul>
                                {writingPosts.length > 0 ? writingPosts.map(p => (
                                    <li><a href={`/blog/${p.id}/`}>{p.data.title}</a></li>
                                )) : (
                                    <li><a href="/blog">更多寫作文章...</a></li>
                                )}
                            </ul>
                        </div>
                        
                        <!-- AI -->
                        <div class="list-column">
                            <h3>🤖 AI 工作流</h3>
                            <ul>
                                {aiPosts.length > 0 ? aiPosts.map(p => (
                                    <li><a href={`/blog/${p.id}/`}>{p.data.title}</a></li>
                                )) : (
                                    <li><a href="/blog">更多 AI 文章...</a></li>
                                )}
                            </ul>
                        </div>
                        
                        <!-- Personal Brand -->
                        <div class="list-column">
                            <h3>🚀 個人品牌</h3>
                            <ul>
                                {brandPosts.length > 0 ? brandPosts.map(p => (
                                    <li><a href={`/blog/${p.id}/`}>{p.data.title}</a></li>
                                )) : (
                                    <li><a href="/blog">更多品牌文章...</a></li>
                                )}
                            </ul>
                        </div>
                    </div>
                </div>
            </div>

		</main>
		<Footer />
	</body>
</html>
