---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { getCollection } from 'astro:content';
import FormattedDate from '../components/FormattedDate.astro';

// Get all posts
const allPosts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

// Latest 3 posts
const posts = allPosts.slice(0, 3);

// Helper to get main tag
const getMainTag = (post) => {
    if (post.data.tags && post.data.tags.length > 0) return post.data.tags[0];
    return 'Article';
};
---

<!doctype html>
<html lang="zh-Hant">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
            /* Ali Abdaal Inspired Styles */
            :root {
                --ali-accent: #2563EB;
                --ali-bg: #FFFFFF;
                --ali-text: #171717;
            }

            .container {
                max-width: 1200px;
                margin: 0 auto;
                padding: 0 1.5rem;
            }

            /* HERO SECTION */
            .hero {
                padding: 6rem 0;
                display: grid;
                grid-template-columns: 1.2fr 1fr;
                gap: 4rem;
                align-items: center;
            }
            @media (max-width: 900px) {
                .hero {
                    grid-template-columns: 1fr;
                    text-align: center;
                    padding: 4rem 0;
                }
                .hero-image { order: -1; margin-bottom: 2rem; }
            }

            .hero-content h1 {
                font-family: var(--font-heading);
                font-size: 3.5rem;
                font-weight: 800;
                line-height: 1.1;
                margin-bottom: 1.5rem;
                color: var(--black);
            }
            .hero-content p {
                font-family: var(--font-body);
                font-size: 1.25rem;
                line-height: 1.6;
                color: var(--gray-dark);
                margin-bottom: 2.5rem;
                max-width: 600px;
            }

            .hero-image img {
                width: 100%;
                border-radius: 12px;
                transform: rotate(2deg); 
                transition: transform 0.3s ease;
                box-shadow: 20px 20px 0 var(--color-warm-bg); 
            }
            .hero-image:hover img {
                transform: rotate(0deg);
            }
            
            /* Custom Beautiful Newsletter Form */
            .hero-newsletter {
                background: white;
                padding: 0;
                max-width: 500px;
            }
            @media (max-width: 900px) { .hero-newsletter { margin: 0 auto; } }
            
            .newsletter-wrapper {
                display: flex;
                gap: 10px;
                flex-wrap: wrap;
            }
            .newsletter-input {
                flex: 1;
                min-width: 200px;
                padding: 1rem 1.2rem;
                border: 2px solid #EEE;
                border-radius: 8px;
                font-size: 1rem;
                transition: border-color 0.2s;
                background: #f9f9f9;
            }
            .newsletter-input:focus {
                outline: none;
                border-color: var(--color-red);
                background: white;
            }
            .newsletter-btn {
                background: var(--color-red);
                color: white;
                border: none;
                padding: 1rem 1.5rem;
                border-radius: 8px;
                font-weight: 700;
                cursor: pointer;
                transition: all 0.2s;
                font-size: 1rem;
            }
            .newsletter-btn:hover {
                background: #b91c1c;
                transform: translateY(-2px);
                box-shadow: 0 4px 12px rgba(220, 38, 38, 0.3);
            }
            .newsletter-caption {
                font-size: 0.9rem;
                color: #666;
                margin-top: 0.8rem;
                font-weight: 500;
            }


            /* SECTIONS */
            .section-header {
                text-align: center;
                margin-bottom: 3rem; 
            }
            .section-header h2 {
                font-size: 2.2rem;
                font-weight: 700;
                margin-bottom: 0.5rem;
                color: var(--color-primary);
            }
            .section-header p {
                color: #666;
                font-size: 1.1rem;
                margin-bottom: 0;
            }
            
            /* SERVICES (Centered Title, Left Red Border) */
            .grid-section {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
                gap: 2rem;
                margin-bottom: 6rem; /* Same space as Latest Articles */
            }
            .service-card {
                padding: 2rem;
                background: white;
                /* Left Red Border Only */
                border-left: 4px solid var(--color-red);
                border-top: 1px solid #eee;
                border-right: 1px solid #eee;
                border-bottom: 1px solid #eee;
                border-radius: 4px;
                box-shadow: 0 4px 6px rgba(0,0,0,0.05);
                transition: transform 0.2s, box-shadow 0.2s;
            }
            .service-card:hover {
                transform: translateY(-4px);
                box-shadow: 0 10px 20px rgba(0, 0, 0, 0.08);
                /* Do NOT change border color to full red, keep left only */
            }
            .service-card h3 {
                margin-top: 0;
                font-size: 1.4rem;
                color: var(--color-primary);
                margin-bottom: 1rem;
                font-weight: 700;
            }
            .service-card p {
                color: var(--gray-dark);
                line-height: 1.6;
                margin: 0;
            }

            /* LATEST ARTICLES */
            .latest-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
                gap: 2rem;
                margin-bottom: 6rem;
            }
            .article-card {
                background: white;
                border: 1px solid #f0f0f0;
                border-radius: 8px;
                overflow: hidden;
                transition: transform 0.2s, box-shadow 0.2s;
                display: flex;
                flex-direction: column;
                height: 100%;
            }
            .article-card:hover {
                transform: translateY(-4px);
                box-shadow: 0 10px 30px rgba(0,0,0,0.08);
            }
            .article-image {
                height: 200px;
                overflow: hidden;
            }
            .article-image img {
                width: 100%;
                height: 100%;
                object-fit: cover;
            }
            .article-content {
                padding: 1.5rem;
                flex: 1;
                display: flex;
                flex-direction: column;
            }
            .article-tag {
                font-size: 0.75rem;
                text-transform: uppercase;
                letter-spacing: 0.05em;
                color: var(--color-red);
                font-weight: bold;
                margin-bottom: 0.5rem;
                display: block;
            }
            .article-title {
                font-size: 1.25rem;
                font-weight: 700;
                margin: 0 0 0.5rem 0;
                line-height: 1.4;
                color: var(--black);
            }
            .article-date {
                font-size: 0.85rem;
                color: #888;
                margin-top: auto; /* Push date to bottom */
            }

            /* START HERE (Original Layout + Topics) */
            .featured-section {
                background: var(--color-warm-bg);
                padding: 6rem 0;
                margin: 4rem 0;
                text-align: center; /* Centered like original */
            }
            
            .topics-grid {
                display: flex;
                justify-content: center;
                gap: 1.5rem;
                margin-top: 2rem;
                flex-wrap: wrap;
            }
            .topic-btn {
                background: white;
                color: var(--color-primary);
                padding: 1rem 2rem;
                border-radius: 50px;
                text-decoration: none;
                font-weight: 600;
                box-shadow: 0 4px 10px rgba(0,0,0,0.05);
                transition: all 0.2s;
                border: 1px solid rgba(0,0,0,0.05);
            }
            .topic-btn:hover {
                transform: translateY(-2px);
                box-shadow: 0 10px 20px rgba(0,0,0,0.08);
                color: var(--color-red);
            }

		</style>
	</head>
	<body>
		<Header />
		<main>
            
            <!-- HERO -->
            <div class="container hero">
                <div class="hero-content">
                    <h1>Hey, I'm Vista 👋</h1>
                    <p>
                        我是<b>講師、作家與顧問</b>。協助你透過 <span style="color:var(--color-red); font-weight:bold;">AI 工具</span>與<span style="color:var(--color-red); font-weight:bold;">內容策略</span>，建立高效的個人知識系統與商業影響力。
                    </p>
                    
                    <!-- Beautiful Custom Form using Substack Action -->
                    <div class="hero-newsletter">
                         <form action="https://iamvista.substack.com/api/v1/free?nojs=true" method="post" target="_blank" class="newsletter-wrapper">
                            <input type="email" name="email" placeholder="輸入你的 E-mail⋯⋯" class="newsletter-input" required />
                            <button type="submit" class="newsletter-btn">免費訂閱 &rarr;</button>
                            <!-- Hidden referrer -->
                            <input type="hidden" name="first_url" value="https://vista.tw" />
                        </form>
                        <p class="newsletter-caption">加入 15,541+ 位讀者的行列，獲得最新的 AI 與寫作洞察。</p>
                    </div>
                </div>
                
                <div class="hero-image">
                    <img src="/profile.jpg" alt="Vista Cheng" width="600" height="600" />
                </div>
            </div>
            
            <!-- SERVICES (Restored & Styled) -->
            <div class="container">
                 <div class="section-header">
                     <h2>核心服務</h2>
                     <p>協助你從輸入到輸出，建立完整的知識體系</p>
                 </div>
                 <div class="grid-section">
                    <div class="service-card">
                        <h3>✍️ 寫作陪伴</h3>
                        <p>拆解寫作焦慮，建立穩定產出的原子習慣。從部落格到出書，陪你走過創作長路。</p>
                    </div>
                    <div class="service-card">
                        <h3>🤖 AI 工作流</h3>
                        <p>不只是下指令，而是打造專屬於你的 AI 協作系統。提升 10 倍效率，專注在更有價值的事。</p>
                    </div>
                    <div class="service-card">
                        <h3>💼 企業內訓</h3>
                        <p>為企業團隊導入內容行銷與數位工具思維，提升組織整體的溝通與生產力。</p>
                    </div>
                </div>
            </div>

            <!-- LATEST ARTICLES -->
            <div class="container">
                <div class="section-header">
                    <h2>最新文章</h2>
                    <p>探索 AI、寫作與自我成長的最新觀點</p>
                </div>
                
                <div class="latest-grid">
                    {
                        posts.map((post) => (
                            <div class="article-card">
                                <a href={`/blog/${post.id}/`} style="text-decoration:none; display:flex; flex-direction:column; height:100%;">
                                    <div class="article-image">
                                        <img src={post.data.heroImage} alt="" />
                                    </div>
                                    <div class="article-content">
                                        <span class="article-tag">{getMainTag(post)}</span>
                                        <h3 class="article-title">{post.data.title}</h3>
                                        <FormattedDate date={post.data.pubDate} class="article-date" />
                                    </div>
                                </a>
                            </div>
                        ))
                    }
                </div>
                
                <div style="text-align: center; margin-bottom: 6rem;">
                     <a href="/blog" style="font-weight: 600; text-decoration: none; color: var(--color-black); border-bottom: 2px solid var(--color-red); padding-bottom: 2px;">閱讀所有文章 &rarr;</a>
                </div>
            </div>

            <!-- START HERE (Original Style + Link to Topics) -->
            <div class="featured-section">
                <div class="container">
                     <h2 style="font-size: 2.5rem; margin-bottom: 1.5rem; font-family: var(--font-heading);">從這裡開始</h2>
                     <p style="font-size: 1.25rem; line-height: 1.6; color: #444; margin: 0 auto 2.5rem auto; max-width: 700px;">
                         如果你是第一次來到這裡，我為你整理了一份導覽指南。這是我精選的熱門文章列表，涵蓋寫作技巧、AI 應用與個人品牌的精華內容。
                     </p>
                     
                     <div class="topics-grid">
                        <a href="/start-here" class="topic-btn" style="background:#000; color:#FFF; display:flex; align-items:center; gap:0.5rem;">
                            <span>開始探索完整指南</span>
                            <span>&rarr;</span>
                        </a>
                        <a href="/topic/writing" class="topic-btn">✍️ 文案寫作</a>
                        <a href="/topic/ai" class="topic-btn">🤖 AI 應用</a>
                        <a href="/topic/personal-brand" class="topic-btn">🚀 個人品牌</a>
                     </div>
                </div>
            </div>

		</main>
		<Footer />
	</body>
</html>
