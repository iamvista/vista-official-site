---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="zh-Hant">
	<head>
		<BaseHead title={`Podcast: 開外掛的人 - ${SITE_TITLE}`} description="拒絕平庸的勞力堆疊。專為高企圖心工作者打造的實戰 Podcast。" />
        <!-- Font Awesome for Brand Icons -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
		<style>
            /* Premium Magazine Style */
            :root {
                --font-serif: "Noto Serif TC", "Songti TC", serif;
                --color-gold: #c5a365;
            }

            .podcast-page {
                background: #fdfbf7; /* Very warm paper-like bg */
            }

            .hero-section {
                padding: 6rem 1rem 4rem;
                max-width: 1100px;
                margin: 0 auto;
                display: flex;
                align-items: center;
                gap: 4rem;
            }
            
            .hero-content {
                flex: 1;
            }

            .hero-image {
                flex: 0 0 350px;
                position: relative;
            }
            .hero-image img, .hero-image .placeholder {
                width: 100%;
                aspect-ratio: 1/1;
                border-radius: 4px;
                box-shadow: 20px 20px 0 var(--color-gold); /* Offset solid shadow */
                object-fit: cover;
                background: #333;
                display: flex;
                align-items: center;
                justify-content: center;
                color: white;
                font-size: 5rem;
            }
            
            h1.main-title { 
                font-family: var(--font-serif);
                font-size: 4rem; 
                line-height: 1.1;
                margin-bottom: 1.5rem; 
                color: #1a1a1a;
                font-weight: 900;
                letter-spacing: -0.03em;
            }
            
            .tagline {
                font-family: var(--font-serif);
                font-size: 1.5rem;
                color: var(--color-gold);
                margin-bottom: 2rem;
                font-style: italic;
                position: relative;
                padding-left: 1rem;
                border-left: 3px solid var(--color-red);
            }

            .description {
                font-size: 1.1rem;
                color: #555;
                line-height: 1.8;
                margin-bottom: 3rem;
            }

            /* Brand Buttons */
            .listen-on {
                display: flex;
                gap: 1rem;
                flex-wrap: wrap;
            }
            .btn-platform {
                display: inline-flex;
                align-items: center;
                gap: 0.75rem;
                padding: 0.8rem 1.5rem;
                border-radius: 50px;
                text-decoration: none;
                font-weight: 600;
                font-size: 0.95rem;
                transition: transform 0.2s, box-shadow 0.2s;
                color: white;
            }
            .btn-apple { background: #000; }
            .btn-spotify { background: #1DB954; }
            .btn-soundon { background: #FACC15; color: #000; }
            
            .btn-platform:hover {
                transform: translateY(-3px);
                box-shadow: 0 10px 20px rgba(0,0,0,0.15);
            }

            /* Segments - Elegant Grid */
            .segments-container {
                max-width: 1100px;
                margin: 6rem auto;
                padding: 0 1rem;
            }
            .segments-title {
                text-align: center;
                font-family: var(--font-serif);
                font-size: 2.5rem;
                margin-bottom: 4rem;
                position: relative;
                display: inline-block;
                width: 100%;
            }
            .segments-title::after {
                content: '';
                display: block;
                width: 60px;
                height: 3px;
                background: var(--color-red);
                margin: 1rem auto 0;
            }

            .segment-row {
                display: flex;
                gap: 4rem;
                margin-bottom: 4rem;
                align-items: flex-start;
            }
            .segment-icon {
                font-size: 3rem;
                color: var(--color-red);
                background: white;
                width: 80px;
                height: 80px;
                display: flex;
                align-items: center;
                justify-content: center;
                border-radius: 50%;
                box-shadow: 0 10px 20px rgba(0,0,0,0.05);
                flex-shrink: 0;
            }
            .segment-content h3 {
                font-family: var(--font-serif);
                font-size: 2rem;
                margin-bottom: 1rem;
                color: #1a1a1a;
            }
            .segment-content p {
                font-size: 1.1rem;
                color: #666;
                line-height: 1.8;
            }

            /* Embed Area - Elegant */
            .player-area {
                background: white;
                color: var(--color-primary);
                padding: 6rem 1rem;
                text-align: center;
                border-top: 1px solid #eee;
            }
            .player-placeholder {
                max-width: 800px;
                height: 200px;
                background: #fafafa;
                margin: 2rem auto 0;
                display: flex;
                align-items: center;
                justify-content: center;
                border: 1px solid #e5e5e5;
                border-radius: 8px;
                color: #999;
                font-family: var(--font-serif);
                font-style: italic;
            }

            /* Newsletter - Refined Elegant */
            .newsletter-elegant {
                background: var(--color-warm-bg); /* Soft Cream */
                color: var(--color-primary);
                padding: 6rem 1rem;
                text-align: center;
                border: 2px solid var(--gray-light); /* Subtle frame */
                margin: 0 auto; /* Ensure full width or contained? Main has padding? No. */
                border-left: none;
                border-right: none;
            }
            
            .cta-button-elegant {
                        display: inline-block;
                        background: var(--color-red);
                        color: white !important;
                        text-decoration: none;
                        padding: 1rem 3rem;
                        border-radius: 50px; /* Soft Pill */
                        font-weight: 600;
                        letter-spacing: 0.5px;
                        box-shadow: 0 4px 10px rgba(185, 28, 28, 0.2); /* Soft Red Shadow */
                        transition: transform 0.2s;
            }
            .cta-button-elegant:hover {
                transform: translateY(-2px);
                box-shadow: 0 6px 15px rgba(185, 28, 28, 0.3);
            }

            @media (max-width: 768px) {
                .hero-section { flex-direction: column; gap: 2rem;}
                .hero-image { flex: auto; width: 100%; max-width: 300px; margin: 0 auto;}
                h1.main-title { font-size: 2.5rem; }
                .segment-row { flex-direction: column; gap: 1rem; text-align: center;}
                .segment-icon { margin: 0 auto; }
            }
		</style>
	</head>
	<body class="podcast-page">
		<Header />
		<main>
            <!-- Hero -->
            <section class="hero-section">
                <!-- ... Content Unchanged ... -->
                <div class="hero-content">
                    <div class="tagline">The Power-Up People</div>
                    <h1 class="main-title">開外掛的人</h1>
                    <p class="description">
                        拒絕平庸的勞力堆疊。這是一個專為高企圖心工作者打造的實戰節目。<br>
                        我們不聊理論，只拆解高手的 SOP 與底層邏輯，讓你在職場與人生中，擁有不公平的優勢。
                    </p>
                    <div class="listen-on">
                        <a href="#" class="btn-platform btn-apple"><i class="fa-brands fa-apple"></i> Apple Podcasts</a>
                        <a href="#" class="btn-platform btn-spotify"><i class="fa-brands fa-spotify"></i> Spotify</a>
                        <a href="#" class="btn-platform btn-soundon"><i class="fa-solid fa-play"></i> SoundOn</a>
                    </div>
                </div>
                <div class="hero-image">
                    <div class="placeholder">🎙️</div>
                </div>
            </section>

            <!-- Segments -->
            <section class="segments-container">
               <!-- ... Content Unchanged ... -->
                <div class="segments-title">節目單元</div>
                
                <div class="segment-row">
                    <div class="segment-icon"><i class="fa-solid fa-bolt"></i></div>
                    <div class="segment-content">
                        <h3>一招開掛 (One Trick)</h3>
                        <p>不講廢話、不談空泛理論。每集針對一個具體職場場景（如提案、會議、談判），直接提供 3 個可執行的外掛 SOP。聽完這集，你就能直接應用，拿回主控權。</p>
                    </div>
                </div>

                <div class="segment-row">
                    <div class="segment-icon"><i class="fa-solid fa-user-secret"></i></div>
                    <div class="segment-content">
                        <h3>對話嘉賓 (The Hacker)</h3>
                        <p>邀請各領域具有實戰經驗的強者。挖掘他們如何利用工具、思維模型與底層邏輯，駭進產業核心。聽聽他們是如何取得那份「不公平優勢」。</p>
                    </div>
                </div>
            </section>

            <!-- Player -->
            <section class="player-area">
                <h2 style="font-family: var(--font-serif); font-size: 2rem; color: #333;">最新單集搶先聽</h2>
                <div class="player-placeholder">
                    [ 預留嵌入播放器區塊：請提供 SoundOn / Firstory Embed Code ]
                </div>
            </section>

            <!-- Call to Action -->
            <section class="newsletter-elegant">
                 <h2 style="font-family: var(--font-serif); font-size: 2.5rem; margin-bottom: 1.5rem; color: #1a1a1a;">不用做筆記，我寄給你</h2>
                 <p style="font-size: 1.2rem; margin-bottom: 3rem; max-width: 600px; margin-left: auto; margin-right: auto; color: #555;">
                    每週一封電子報，為你整理最關鍵的 Cheat Sheet。<br>
                    直接複製高手的思考路徑，存入你的大腦。
                 </p>
                 <a href="https://iamvista.substack.com/" target="_blank" class="cta-button-elegant">
                        免費訂閱 Substack
                </a>
            </section>
		</main>
		<Footer />
	</body>
</html>
